{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch dottalkpp (CMake target, MSVC)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "stopAtEntry": false
    }
  ]
}