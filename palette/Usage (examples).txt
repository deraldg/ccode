Usage (examples):

MSVC (Visual Studio)
cmake -S . -B build-msvc -G "Visual Studio 17 2022" -A x64 && cmake --build build-msvc --config Release

MinGW (MSYS2/Win):
cmake -S . -B build-mingw -G Ninja -DCMAKE_C_COMPILER=gcc -DCMAKE_CXX_COMPILER=g++ && cmake --build build-mingw -v

WSL / Linux:
cmake -S . -B build -G Ninja && cmake --build build -v

a. Want me to add CMakePresets.json for one-click configs: msvc, mingw, wsl?
b. Should I generate a minimal PowerShell + Bash build runner that picks the right preset and runs the app?

Thinking
ChatGPT can make mistakes. Check important info.