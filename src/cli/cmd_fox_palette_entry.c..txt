// ccode/src/cli/cmd_fox_palette_entry.cpp
// why: provides the *definition* that shell.cpp links against
#include <sstream>
#include <tvision/tv.h>
#include "cmd_fox_palette_command.h"
#include "cmd_fox_palette.h"        // from ccode/palette (TTestApp)

namespace xbase { class DbArea; }

static void run_palette_app() {
    // keep UI logic centralized in your palette code
    TTestApp app;
    app.run();
}

void cmd_FOX_PALETTE(xbase::DbArea& /*area*/, std::istringstream& /*args*/) {
    run_palette_app();
}
