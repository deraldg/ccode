nice—shipping bits again! here’s what we’ve actually landed:

# what’s in the build now

* # Build system & layout

  * Split into two libs + one exe: `xbase` (core DBF), `xindex` (indexing), `dottalkpp` (CLI).
  * Clean CMake: no circular links, excludes legacy/experimental files (`simple_index.cpp`, `dbarea_introspection.cpp`, accidental `src_cli_cmd_status.cpp`).
  * Config banner shows paths + “Indexing: ON”.
  * Added a handy `tools/find-dupes.ps1` and used it to chase down duplicate handlers and the `DbArea` alias conflict.

* # Adapter layer (xindex ↔ DbArea)

  * `xindex/dbarea_adapt.*` now bridges to your `xbase::DbArea`:

    * `db_record_count`, `db_recno`, `db_get_string`, `db_get_double`, `db_is_deleted`.
    * Optional extras (gated by macros): `db_filename`, `db_record_length`.
  * We defined `XINDEX_ADAPT_HAS_FILENAME` and `XINDEX_ADAPT_HAS_RECLEN` for the **xindex** target so those paths compile.

* # `DbArea` small API surface (new getters)

  * New members on `xbase::DbArea`:

    * `int recordLength() const noexcept;`
    * `std::string filename() const;`
  * For now they’re **stubbed** in `src/xbase/dbarea.cpp` (return 0/empty). This unblocks the build and `STATUS` formatting, and we can wire them to your real DBF metadata when you’re ready.

* # CLI features you tested working end-to-end

  * Table lifecycle: `CREATE`, `USE` (implicit), `APPEND` (+ variations), `COUNT`, `GOTO`, `RECNO`, `DISPLAY`, `REPLACE`, `DELETE`, `RECALL`, `PACK`.
  * Indexing path:

    * `INDEX ON LAST_NAME TAG L` creates `.inx` and integrates with the in-memory manager.
    * `SEEK LAST_NAME "ZEKE"` succeeded.
    * After `PACK` we warn “indexes are stale; rebuild and SET INDEX.”
  * Reporting:

    * `STRUCT` prints schema cleanly.
    * `STATUS` prints:

      * records ✓,
      * recno ✓,
      * order direction (ASC/DESC) ✓,
      * active tag (none when not set) ✓,
      * loaded tags with expression (e.g., `L -> LAST_NAME`) ✓,
      * filename/bytes-per-rec show placeholders for now (stubs).

* # Index implementation bits

  * We dropped the legacy `simple_index.cpp` that expected the old `DbArea` API.
  * Added a minimal “build-and-save” path with shims to satisfy `cmd_INDEX` and friends. (You saw benign LNK4006 “already defined” warnings once—harmless but we’ll tidy them.)

---

# what’s still intentionally stubbed / to tighten up

1. **`DbArea::filename()` + `recordLength()`**

   * Wire these to your real DBF internals so `STATUS` shows actual values and any consumers don’t guess.

2. **Link warning cleanup**

   * Keep **one** definition of `SimpleIndex::build_and_save` (drop either the stub or the link-shim) to silence the duplicate-definition linker warnings.

3. **`SET INDEX` ergonomics**

   * We adjusted `cmd_setindex` to use the `IndexManager` you already have; worth a quick run-through with multiple tags/files to confirm UX.

4. **Index lifecycle after mutations**

   * Hook `PACK/DELETE/RECALL/REPLACE/APPEND` notifications to rebuild/refresh the active tag automatically or on demand (we already print “stale” notice—next is the automatic or explicit rebuild path).

5. **Finish the TODO you asked me to track**

   * Revisit the earlier “comment-out recno in `STATUS`” contingency. It’s working now via the adapter; once filename/reclen are wired, we can formalize and remove that safety net entirely.

---

# quick sanity checklist you can try now

* `STATUS` on a real DBF (after we wire filename/reclen) should show:

  * File path, record count, current recno, bytes/rec, order, active tag, loaded tags+expr.
* `INDEX ON <field> TAG <name>` then `SET INDEX TO <name>` then `STATUS` → active tag should flip to `<name>`, and `LIST` should reflect the order.
* `PACK` → either auto-rebuild the active tag (if we decide to) or `INDEX` again + `SET INDEX`.

If you want, I can give you the tiny patch to wire `recordLength()`/`filename()` to your `DbfFile` header now—just tell me where you store the header/metadata (member names/types) and I’ll drop in the exact code.
